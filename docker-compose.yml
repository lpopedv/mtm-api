version: '3'

services:
  mtm-pg:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=mtm
      - POSTGRESQL_PASSWORD=mtm
      - POSTGRESQL_DATABASE=mtm